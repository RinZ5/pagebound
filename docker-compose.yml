services:
  pagebound:
    build: .
    ports:
      - "8082:8082"
    volumes:
      - pagebound_data:/data
    environment:
      LISTEN_ADDR: ":8082"
    restart: unless-stopped

volumes:
  pagebound_data:
